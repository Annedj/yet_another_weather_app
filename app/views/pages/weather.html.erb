<div class="top-bar">
  <div>
    <h1 id="city">Hello!</h1>
    <p class="today-date">Today's <%= Date.today.strftime("%B %d, %A") %>.</p>
  </div>
  <div class="inputs">
    <%= link_to 'Current location', '', class: 'btn btn-primary round', id: 'get-location' %>
    <p>or </p>
    <input type="search" id="address-input" placeholder="Enter a city name" />
    <input type="submit" id="address-btn" value="Get the weather" class="btn btn-primary ml-3">
  </div>
<!--   <div class="search-bar">
    <p>Another city?</p>
  </div> -->
</div>

<div class="d-flex">
    <%= render 'components/side_bar_days' %>
  <div class="weather-container">

    <%= render 'components/weather_details' %>
  </div>
</div>
